# Dev only. Empty base = same-origin /api/*, which the Vite proxy in
# vite.config.ts forwards to the Worker. Without this the browser calls the
# Worker directly from localhost and CORS drops every response, leaving the
# ticker and the market-session check with no data.
VITE_BOARD_API_BASE=
