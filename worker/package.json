{
  "name": "stock-predict-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute stock-predict-board --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute stock-predict-board --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.0.0"
  }
}
