/**
 * How many community posts the dashboard's weekly-hot list shows.
 *
 * The same count on every screen: the strip runs the full width, so four rows fit
 * on a phone. An earlier version trimmed to two while it shared a grid row with
 * the chat strip.
 */
export const COMMUNITY_HOT_COUNT = 4;
